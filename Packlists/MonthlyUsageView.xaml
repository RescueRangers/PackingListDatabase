<Window x:Class="Packlists.MonthlyUsageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        xmlns:collections="clr-namespace:System.Collections;assembly=mscorlib"
        xmlns:converters="clr-namespace:Packlists.Converters"
        mc:Ignorable="d"
        Title="Monthly reports" Height="450" Width="800"
        DataContext="{Binding MonthlyReport, Source={StaticResource Locator}}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <DataGrid ItemsSource="{Binding Report, IsAsync=True}"
                  AutoGenerateColumns="True"
                  AutoGeneratingColumn="Dg_OnAutoGeneratingColumn"
                  IsReadOnly="True"
                  EnableColumnVirtualization="True"
                  EnableRowVirtualization="True"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  MaxWidth="2500"
                  MaxHeight="1500"/>
    </Grid>
</Window>
